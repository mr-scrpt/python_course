# Виртуальные окружения (самое важное!)
# Никогда не добавляйте venv в Git. Он может весить сотни мегабайт
# и содержит пути, специфичные для вашего компьютера.
/venv/
/.venv/
/env/
/.env/
/activate
/bin/
/include/
/lib/
/lib64/
/share/
/scripts/

# Кэш и байт-код Python
# Эти файлы создаются автоматически при запуске .py файлов
__pycache__/
*.pyc
*.pyo
*.pyd

# Файлы IDE и редакторов
# Настройки вашего редактора не должны быть в общем репозитории
.idea/
.vscode/
*.sublime-workspace

# Файлы тестирования
# Кэш и отчеты о покрытии кода тестами
.pytest_cache/
.coverage
htmlcov/

# Секреты и переменные окружения (КРИТИЧЕСКИ ВАЖНО!)
# Никогда не добавляйте файлы с паролями, ключами API и т.д. в Git
.env
*.env

# Логи и временные базы данных
*.log
*.db
*.sqlite3

# Сгенерированные файлы при сборке пакета
/build/
/dist/
*.egg-info/
